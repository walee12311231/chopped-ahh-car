; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200

; Memory Partitioning: Gives you ~3 M for code (huge_app) 
; by removing the OTA (Over-The-Air) update space.
board_build.partitions = huge_app.csv

; Map: 128×128 grid at 16 mm/cell → 2.048 m (~80″) coverage, fits 36×36″ maze
; Uses 32 KB RAM instead of 128 KB — leaves room for WiFi stack
build_flags =
    -DSLAM_MAP_SIZE=128
    -DSLAM_MAP_SCALE=0.016f

; Library Dependencies
lib_deps =
    adafruit/Adafruit PWM Servo Driver Library @ ^3.0.2